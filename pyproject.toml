[project]
name = "bh-scripts"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "SaulLawliet", email = "october.sunbathe@gmail.com" }
]
requires-python = ">=3.14"
dependencies = [
    "fake-useragent>=2.2.0",
]

[project.scripts]
bh-scripts = "bh_scripts:main"

[build-system]
requires = ["uv_build>=0.12.7,<0.13.0"]
build-backend = "uv_build"
